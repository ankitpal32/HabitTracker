# (Optional) Backend API base URL.
# When frontend and backend are deployed in the same Vercel project, leave empty to use relative /api paths.
# If backend is hosted on a separate domain, specify the URL:
# VITE_API_URL=https://your-api-domain.vercel.app

# (Optional) Backend API base URL.
# - In production on Vercel: Leave empty so it automatically uses relative /api endpoints.
# - In local development: Leave empty (defaults to http://localhost:3000) or specify custom backend.
# VITE_API_URL=
